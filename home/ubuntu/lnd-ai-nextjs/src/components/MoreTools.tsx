"use client";
import React from 'react';

// This component is no longer used to render the grid of tools.
// The grid of links is now on the /more-tools/page.tsx file itself.
// This component is being kept as a shell to prevent breaking any
// potential imports, but it can be safely removed in the future.
const MoreTools = () => {
    return <div>More Tools</div>;
};

export default MoreTools;
